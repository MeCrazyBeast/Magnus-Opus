package gData;
import java.util.Map;
import static java.util.Map.entry;
public class Bloodlines {
    Map<String, String> rBloodLine = Map.ofEntries(
            entry("E", "Mixed Blood"),
            entry("D", "Quarter Blood"),
            entry("C", "Half Blood"),
            entry("B", "Pure Blood"),
            entry("A", "Royal Blood"),
            entry("S", "Primordial Blood")
    );
}
